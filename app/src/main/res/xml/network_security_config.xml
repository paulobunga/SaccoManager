<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Global: enforce HTTPS only, no cleartext allowed -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Production API domain with certificate pinning -->
    <!-- TODO: Replace placeholder pins with real SHA-256 hashes before production release -->
    <!-- To generate: openssl s_client -connect api.sacco.org:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | base64 -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.sacco.org</domain>
        <pin-set expiration="2027-01-01">
            <!-- PLACEHOLDER: Replace with actual certificate pin before production -->
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <!-- Backup pin (required) — also a placeholder -->
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
    </domain-config>

    <!-- Debug: allow cleartext to localhost and Android emulator host -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
